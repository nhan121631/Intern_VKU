version: '3.8'

services:
  gemini-api:
    build: .
    container_name: gemini-api
    ports:
      - "5001:5001"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./my-backend-key.pem:/app/my-backend-key.pem:ro
      - ./.env:/app/.env:ro
    restart: unless-stopped
